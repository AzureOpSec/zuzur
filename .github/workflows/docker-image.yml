name: Python application

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "jaenmirmaslup" --password-stdin
        docker image build -t jaenmirmaslup/masupilami:latest .
        docker push jaenmirmaslup/masupilami:latest
