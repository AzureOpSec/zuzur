name: Python application

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "genisadamin" --password-stdin
        docker image build -t genisadamin/biglatencytest:latest   .
        docker push genisadamin/biglatencytest:latest
